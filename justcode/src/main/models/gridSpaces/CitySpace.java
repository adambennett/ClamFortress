package main.models.gridSpaces;

import main.game.regions.*;

public class CitySpace extends AbstractGridSpace {
    public CitySpace(int x, int y, AbstractRegion biome) {
        super(x, y, biome);
    }
}
