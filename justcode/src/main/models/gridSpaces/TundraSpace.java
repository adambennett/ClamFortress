package main.models.gridSpaces;

import main.game.regions.*;

public class TundraSpace extends AbstractGridSpace {
    public TundraSpace(int x, int y, AbstractRegion biome) {
        super(x, y, biome);
    }
}
